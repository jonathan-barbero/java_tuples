package ar.com.job.java_tuples.comparator;

import ar.com.job.java_tuples.TP;

import java.util.Comparator;

public class TPComparator implements Comparator<TP> {
    @Override
    public int compare(TP o1, TP o2) {
        return 0;
    }
}
